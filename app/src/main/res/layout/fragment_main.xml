<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.gmail.lgelberger.popularmovies.MainActivityFragment"
    tools:showIn="@layout/activity_main">

    <!--
    removed
    android:numColumns="2"  Replaced it with GridViewStyle which is number of columns, but it changes
    depending on size/orientation
    -->
    <GridView
        android:id="@+id/gridview_movies"
        style="@style/GridViewStyle"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_gravity="center"
        android:layout_weight="1"
        android:choiceMode="singleChoice"
        android:drawSelectorOnTop="true"
        android:horizontalSpacing="8dp"
        android:verticalSpacing="8dp" />

</LinearLayout>
